apiVersion: 1

apps:
  - type: 'event-listener-panel'
    org_id: 1
    org_name: 'tespkg'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
      isApiKeySet: true
    secureJsonData:
      apiKey: secret-key
